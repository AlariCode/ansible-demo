---
- name: user
  hosts: demo
  tasks:
    - name: Create user
      vars:
        user: alaricode
      user:
        name: "{{ user }}"
        state: present
      become: true
      debugger: always
