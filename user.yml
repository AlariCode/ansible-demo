---
- name: user
  hosts: demo
  vars_prompt:
    - name: user
      prompt: "Введите пользователя"
      private: no
  tasks:
    - name: Create user
      user:
        name: "{{ user }}"
        state: present
      become: true
